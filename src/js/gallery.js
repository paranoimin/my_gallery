var gallery = function(selector, imgs) {
    // console.log("selector : " + selector + " imgs : " + imgs);

    //----------
    // CONSTANT
    //----------
    var IMG_WIDTH = 100;
    var IMG_HEIGHT = 100;

    var IMG_MARGIN_W = 5;
    var IMG_MARGIN_H = 5;

};